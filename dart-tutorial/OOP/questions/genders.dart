/// 3. Write a dart program to create an enum class for gender [male, female, others] and print all values.

enum Gender {
    Male,
    Female,
    Other
}

void main() {
    print(Gender.Male);
    print(Gender.Female);
    print(Gender.Other);
}